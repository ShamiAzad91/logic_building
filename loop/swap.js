const getSwapValue = function(num){
    let first = 0;
    let last = 0;
    let swap = num;
    
    last = temp%10;

    while (temp>10) {
        temp = parseInt(temp/10)
        
    }
    first = temp;

    let 

}